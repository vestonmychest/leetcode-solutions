class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """

        s = s.split(' ')
        s = ' '.join([x for x in s[::-1]])
        return ' '.join(s.split())
